<?xml version="1.0" encoding="utf-8" ?>
<Defs>

  <ThingDef ParentName="GravshipComponentBase">
    <defName>VGE_EmergencyGenerator</defName>
    <label>emergency gravship generator</label>
    <description>An emergency power source capable of fully powering a gravship by adjusting its output to match demand. It is designed to be kept for emergencies, as it incurs high maintenance costs while active that lead to a critical breakdown and EMP explosion if left unmanaged. The more power it needs to generate, the higher the maintenance drain.</description>
    <drawerType>MapMeshAndRealTime</drawerType>
    <graphicData>
      <texPath>Things/Structures/Misc/EmergencyGravshipGenerator/EmergencyGenerator_Off</texPath>
      <graphicClass>Graphic_Single</graphicClass>
      <drawSize>(3,3)</drawSize>
      <shadowData>
        <volume>(1.93,1,1.69)</volume>
        <offset>(-0.03,0,-0.1)</offset>
      </shadowData>
    </graphicData>
    <altitudeLayer>Building</altitudeLayer>
    <blockWind>true</blockWind>
    <passability>PassThroughOnly</passability>
    <pathCost>22</pathCost>
    <fillPercent>0.4</fillPercent>
    <rotatable>false</rotatable>
    <costList>
      <Steel>200</Steel>
<!--      <GravlitePanel>80</GravlitePanel>-->
      <ComponentIndustrial>12</ComponentIndustrial>
      <ComponentSpacer>6</ComponentSpacer>
    </costList>
    <statBases>
      <MaxHitPoints>650</MaxHitPoints>
      <Flammability>0.5</Flammability>
      <Beauty>-40</Beauty>
      <Mass>40</Mass>
      <VGE_MaintenanceSensitivity>0</VGE_MaintenanceSensitivity>
      <VGE_MaintenanceDifficulty>2.5</VGE_MaintenanceDifficulty>
    </statBases>
    <tickerType>Normal</tickerType>
    <canOverlapZones>false</canOverlapZones>
    <size>(3,3)</size>
    <comps>
      <li Class="CompProperties_GravshipFacility">
        <requiresLOS>false</requiresLOS>
        <maxSimultaneous>1</maxSimultaneous>
        <maxDistance>500</maxDistance>
      </li>
      <li Class="VanillaGravshipExpanded2.CompProperties_EmergencyGravshipGenerator">
        <transmitsPower>true</transmitsPower>
        <!-- Both the base power consumption, as well as the global power net power output this generator will maintain. -->
        <basePowerConsumption>-1000</basePowerConsumption>

        <!-- Each rare tick, the emergency generator power output will grow by up to this amount, -->
        <!-- until the grid output has reached a stable amount. Mostly to prevent a jump from 1000 to 99999999999 in an instant. -->
        <maxPowerIncreasePerRareTick>2500</maxPowerIncreasePerRareTick>
        <!-- How many watt days of output this generator can produce before breakdown. -->
        <!-- Basically, the generator will last long enough to fill batteries storing this many Wd. -->
        <maxWattDaysOutput>50000</maxWattDaysOutput>

        <!-- The amount of heat per second pushed by the generator. -->
        <heatPerSecond>
          <points>
            <li>(0, 0)</li>
            <li>(1000, 50)</li>
            <li>(10000, 250)</li>
            <li>(50000, 500)</li>
            <li>(100000, 1500)</li>
          </points>
        </heatPerSecond>
        <!-- If a gravship linkable, the heat will be pushed the same way heat is distributed from a launch or by using weapons, etc. -->
        <!-- If heat can't be pushed to a ship, it'll instead be pushed into the area the generator is at. -->
        <pushHeatIntoEntireGravship>true</pushHeatIntoEntireGravship>

        <!-- After the building breaks down, how many days you'll have to wait until you can use another emergency generator. -->
        <cooldownDaysAfterBreakdown>30</cooldownDaysAfterBreakdown>
        <!-- Chance for fire after breakdown (and size of the fire). Also supports astrofire. Only affects cells occupied by generator. -->
        <fireChanceAfterBreakdown>
          <points>
            <li>(0, 0)</li>
            <li>(1000, 0.05)</li>
            <li>(10000, 0.25)</li>
            <li>(50000, 1)</li>
          </points>
        </fireChanceAfterBreakdown>
        <fireSizeAfterBreakdown>
          <points>
            <li>(0, 0.25)</li>
            <li>(10000, 0.85)</li>
            <li>(50000, 1.25)</li>
            <li>(100000, 2.5)</li>
          </points>
        </fireSizeAfterBreakdown>

        <!-- Explosion on breakdown data. If def is not specified there'll be no explosion. Also includes a lot more data like -->
        <!-- normal explosions (not all), like armor penetration or chance to start fire. All start with "<breakdownExplosion". -->
        <breakdownExplosionDamageDef>EMP</breakdownExplosionDamageDef>
        <breakdownExplosionDamage>50</breakdownExplosionDamage>
        <breakdownExplosionRadius>24.9</breakdownExplosionRadius>
      </li>
      <li Class="CompProperties_Glower">
        <glowRadius>8</glowRadius>
        <glowColor>(90,140,90,0)</glowColor>
      </li>
      <li Class="CompProperties_Interactable">
        <compClass>VanillaGravshipExpanded2.CompInteractable_EmergencyGravshipGenerator</compClass>
        <ticksToActivate>120</ticksToActivate>
        <requiresPower>false</requiresPower>
        <activateTexPath>UI/Gizmos/ExtractAstrofuel_Gizmo</activateTexPath>

        <jobString>activate emergency gravship generator</jobString>
        <onCooldownString>unstable</onCooldownString>

        <targetingParameters>
          <canTargetBuildings>false</canTargetBuildings>
          <canTargetAnimals>false</canTargetAnimals>
          <canTargetMechs>false</canTargetMechs>
          <onlyTargetControlledPawns>true</onlyTargetControlledPawns>
        </targetingParameters>
      </li>
      <li Class="CompProperties_Breakdownable"/>
      <li Class="CompProperties_Stunnable">
        <useLargeEMPEffecter>true</useLargeEMPEffecter>
        <affectedDamageDefs>
          <li>EMP</li>
        </affectedDamageDefs>
      </li>
    </comps>
    <terrainAffordanceNeeded>Substructure</terrainAffordanceNeeded>
    <constructEffect>ConstructMetal</constructEffect>
    <drawPlaceWorkersWhileSelected>true</drawPlaceWorkersWhileSelected>
    <tradeTags>
      <li>GravshipUpgrade</li>
    </tradeTags>
  </ThingDef>

</Defs>